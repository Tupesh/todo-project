pipeline{
    agent any

    environment{

    }

    stages{
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage("Sonarqube quality scan"){

        }
        stage("Docker image create"){

        }
        stage("Trivy image scan"){

        }

        stage("Docker image push"){

        }
        
    }

}